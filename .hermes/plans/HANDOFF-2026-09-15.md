# Handoff ke Hermes Agent — KelasKita 7B

> Tanggal: 2026-09-15 | **FASE 2 selesai** → **Next: FASE 3 (TDD libs, offline)**
> `npx tsc --noEmit` ✅ · `npm run build` ✅

---

## A. SUDAH SELESAI

### A1. AppShell.tsx — REWRITE (breaking)
**Props baru:**
```ts
brand: { logoSrc, logoAlt, title }          // subtitle DIHAPUS
nav: { items: AppShellNav[] }               // replaces nav.left/home/right
notifications?: AppShellNotification[]       // read flag per item, replaces unreadIds
user?: { name, avatar? }                     // email DIHAPUS
onNotificationClick, onSettings, onSignOut   // replaces onMarkAllRead
```
Header: monogram + bell badge + settings gear + avatar. 5-tab bottom nav.
isActive handle nested routes.

### A2. layout.tsx — consumer AppShell
ShellLayout client component, read-state ke localStorage.
Nav: /app Beranda, /app/pengumuman, /app/agenda, /app/kas, /app/poin
User: { name: "Siswa 7B" } placeholder → nanti getContext()

### A3. Halaman 6 route (bahasa Indonesia)
- `/app` Beranda: saldo kas aksen, 2 StatCard, pinned, agenda, top3 leaderboard
- `/app/pengumuman`: list dengan pinned badge
- `/app/agenda`: grouped by date, lokasi + jam
- `/app/kas`: saldo besar, status iuran pribadi, riwayat transaksi
- `/app/poin`: poin saya + papan peringkat, highlight "kamu"
- `/app/settings`: 3 toggle + about + sign-out
- **Dihapus:** browse/, activity/, saved/
### A4. demo-data.ts — SSOT
Types: `AppNotification`, `Announcement`, `AgendaEvent`, `KasTransaction`, `LeaderboardEntry`
Arrays: `NOTIFICATIONS`, `ANNOUNCEMENTS`, `AGENDA`, `KAS_TRANSACTIONS`, `LEADERBOARD`.
Types lama (`FeedItem`, `SavedItem`, `FEED`, `SAVED`) DIHAPUS.

### A5. PWA + Rebrand
- `manifest.json`: name + short_name "KelasKita 7B", start_url `/app`
- `@serwist/next` (bukan next-pwa) di `next.config.mjs` → build generate `sw.js`
- Metadata + appleWebApp title: "KelasKita 7B"
- Icons: `icon-192`, `icon-512`, `icon-maskable-512`, `apple-touch-icon` (placeholder monogram 7B)



---

## B. NEXT — mulai dari sini

### B1. T-13 `lib/format.ts` TDD (SEGERA, offline)
```ts
formatIDR(n: number): string       // "Rp 1.250.000"
monthKeyWIB(date?): string         // "YYYY-MM", pakai UTC+7
monthLabel(m: string): string      // "Sep" (Indonesia)
formatDateID(d: Date): string      // "Sen, 15 Sep"
formatTimeID(d: Date): string      // "07:00"
```
Test: `src/lib/format.test.ts` pakai vitest (`^4.1.11` sudah di devDeps)

### B2. T-14 `lib/policies.ts` TDD (offline)
```ts
canPostAnnouncement(role): HOMEROOM, KETUA
canManageAgenda(role):    HOMEROOM, KETUA, SEKRETARIS
canManageKas(role):       HOMEROOM, BENDAHARA
canGivePoints(role):      HOMEROOM only
canAdmin(role):           HOMEROOM only
canUseApp(role):          role valid (bukan PENDING)
```
Test semua 6 role: HOMEROOM/KETUA/BENDAHARA/SEKRETARIS/ANGGOTA/PENDING

### B3. T-15 `lib/fetcher.ts` + `lib/session.ts`
- `getContext()` → baca session role/name
- `requireApi(policy?)` → guard route handlers
- `apiError(err)` → format error
- `useAppSWR` → SWR wrapper

### B4. FASE 1 (BUTUH ONLINE — Supabase + Google OAuth)
T-4 sampai T-9: Supabase `kelaskita-7b` ap-southeast-1, env.local,
Google OAuth @mutiarabangsa.sch.id, auth code, /login, smoke test

### B5. FASE 4 — Vertical Slices (butuh FASE 1)
T-16..T-23: route handlers + useSWR + ui-primitives, satu commit per slice
4A Beranda, 4B Pengumuman, 4C Agenda, 4D Kas, 4E Poin, 4F Admin Roster

### B6. FASE 5-6 (butuh FASE 1+4)
T-24..T-29: Push VAPID, cron reminder, deploy Vercel, QA, rollout

---

---

## C. CATATAN PENTING

1. AppShell props breaking — pakai props baru saja, props lama sudah dihapus total.
   Lama: brand.subtitle, nav.left/nav.home/nav.right, unreadIds, onMarkAllRead, user.email, profileActions.
2. Styling — jangan pakai style={{}}, hex (bg-[#144d36]), atau raw px. Tokens: bg-forest, bg-deep, text-ink, text-ink-soft, bg-surface, text-acid, border-line, rounded-xl, rounded-2xl, shadow-sm.
3. demo-data.ts — jangan hapus sebelum FASE 4 selesai. Ganti bertahap per slice (4A..4F).
4. PWA — pakai @serwist/next, bukan next-pwa. sw.js otomatis dari build.
5. Build/cache aneh (.next stale) — hapus folder .next lalu ulang: npx tsc --noEmit / npm run build.
6. Vite test: vitest ^4.1.11 sudah siap. T-13..T-15 wajib ada test file .test.ts.
7. T-15 fetcher/session dipakai di FASE 4; jangan mulai 4A sebelum T-15 hijau.



---

## D. PERINTAH CEPAT

```bash
npm run dev        # dev server → /app
npx tsc --noEmit   # typecheck (hapus .next dulu kalau error aneh)
npm run build      # production + sw.js
npm run start      # test offline mode ( airplane mode → shell hidup )
npx vitest run     # jalankan semua test setelah T-13..15 ada
```

**Next action: Hermes Agent mulai dari T-13 `lib/format.ts` TDD.**

