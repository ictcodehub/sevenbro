# PROMPT TRANSFER KE HERMES AGENT — KelasKita 7B

> Copy-paste seluruh bagian di bawah ke Hermes Agent. Jangan edit.

---

## MULAI DARI SINI

Kamu mewarisi proyek KelasKita 7B di `D:\Workspace\sevenbro`. Fase 2 (Shell & PWA) sudah selesai. Teruskan dari **T-13 `lib/format.ts` TDD**.

## RINGKASAN PROYEK

Next.js 15 + React 19 + Tailwind 4 + @serwist/next. App Router. TypeScript strict.

Build: `npm run build` ✅ | Typecheck: `npx tsc --noEmit` ✅ (kalau error, hapus folder `.next` dulu — stale cache).

## FILE YANG SUDAH ADA

```
src/
├── app/layout.tsx              # root metadata KelasKita 7B
├── app/page.tsx                # redirect → /app
├── app/globals.css             # CSS tokens + @theme inline
├── app/app/layout.tsx          # ShellLayout (client wrap AppShell)
├── app/app/page.tsx            # Beranda
├── app/app/pengumuman/page.tsx # Pengumuman
├── app/app/agenda/page.tsx     # Agenda
├── app/app/kas/page.tsx        # Kas
├── app/app/poin/page.tsx       # Poin
├── app/app/settings/page.tsx   # Settings
├── components/AppShell.tsx     # 5-tab bottom nav
├── components/ui-primitives.tsx# SectionHeader, StatCard, ListRow, HeroCard, ProgressCard, EmptyState, Timeline, TimelineItem
├── components/ui/              # shadcn-style: avatar, badge, button, tabs
├── lib/demo-data.ts            # Types + arrays (AppNotification, Announcement, AgendaEvent, KasTransaction, LeaderboardEntry)
├── lib/utils.ts                # cn() helper
└── (public/manifest.json — KelasKita 7B, start_url "/app")
```

## KONVENSI WAJIB

- Styling pakai **CSS tokens saja**: `bg-forest`, `bg-deep`, `bg-page`, `bg-surface`, `text-ink`, `text-ink-soft`, `text-acid`, `border-line`, `rounded-xl`, `rounded-2xl`, `shadow-sm`, `shadow-lg`. Jangan pakai `style={{}}`, hex (`bg-[#...]`), atau raw `px` arbitrer.
- Komponen UI pakai `lucide-react` icons. Jangan tambah dependency icon baru.
- Semua halaman pakai `"use client"`, bahasa Indonesia.
- `AppShell.tsx` props (breaking change):
  ```ts
  brand: { logoSrc, logoAlt, title }
  nav: { items: { href, label, icon }[] }
  notifications?: { id, title, body, time, read }[]
  user?: { name, avatar? }
  onNotificationClick, onSettings, onSignOut
  ```
  Props lama (nav.left/right, unreadIds, onMarkAllRead, brand.subtitle, user.email, profileActions) sudah DIHAPUS — jangan pakai lagi.
- `package.json` sudah punya `vitest ^4.1.11` sebagai devDep. Gunakan `npx vitest run` untuk test.

## TUGAS LANJUTAN (OFFLINE, TDD)

### T-13: `src/lib/format.ts` + `src/lib/format.test.ts`

Buat fungsi:
```ts
export function formatIDR(n: number): string         // "Rp 1.250.000", tanpa desimal
export function monthKeyWIB(date?: Date | string): string  // "YYYY-MM", pakai UTC+7 offset
export function monthLabel(m: string): string          // "Jan", "Feb", ..., "Des"
export function formatDateID(d: Date): string          // "Sen, 15 Sep" (hari + tanggal + bulan singkat)
export function formatTimeID(d: Date): string          // "07:00" (24h, leading zero)
```

Test dengan `vitest`: minimum 5 `expect().toBe()` per function, cover edge case (0, negative, big number, different date).

### T-14: `src/lib/policies.ts` + `src/lib/policies.test.ts`

```ts
export function canPostAnnouncement(role: string): boolean  // HOMEROOM, KETUA
export function canManageAgenda(role: string): boolean       // HOMEROOM, KETUA, SEKRETARIS
export function canManageKas(role: string): boolean          // HOMEROOM, BENDAHARA
export function canGivePoints(role: string): boolean         // HOMEROOM only
export function canAdmin(role: string): boolean              // HOMEROOM only
export function canUseApp(role: string): boolean             // role valid (bukan PENDING)
```

Test semua 6 role: HOMEROOM, KETUA, BENDAHARA, SEKRETARIS, ANGGOTA, PENDING.

### T-15: `src/lib/session.ts` + `src/lib/fetcher.ts`

`session.ts`:
```ts
export async function getContext(): Promise<{ role: string; name: string }>
export function requireApi(policy?: (role: string) => boolean): void
export function apiError(err: unknown): { message: string; code?: string }
```

`fetcher.ts`:
```ts
export function useAppSWR<T>(path: string, policy?: string): { data: T | null; error: boolean; mutate: () => void }
```

Test mock fetch. Jangan start T-16 sampai T-15 hijau.

## SETELAH T-15 (butuh online — TUNDA kalau tanpa internet)

- T-16..T-23: FASE 4 — route handlers + `useAppSWR` per slice (Beranda, Pengumuman, Agenda, Kas, Poin, Roster)
- T-24..T-26: FASE 5 — Push notification VAPID, cron reminder iuran
- T-27..T-29: FASE 6 — Deploy Vercel, QA, rollout

## FILE PENTING UNTUK DIBACA DULU

1. `.hermes/plans/HANDOFF-2026-09-15.md` — handoff lengkap
2. `TODO.md` — checklist 29 task
3. `src/components/AppShell.tsx` — props interface referensi
4. `src/lib/demo-data.ts` — types dan data contoh
5. `src/app/app/layout.tsx` — ShellLayout consumer
6. `src/app/globals.css` — CSS tokens yang tersedia

## VERIFIKASI MINIMAL SETIAP SELESAI TUGAS

```bash
npx tsc --noEmit    # typecheck — kalau error, hapus .next lalu ulang
npm run build       # build production — pastikan 6 route + sw.js
npx vitest run      # test — semua test harus hijau
```

## CATATAN PENTING

- Jangan hapus `src/lib/demo-data.ts` sampai FASE 4 selesai. File ini jadi SSOT sementara.
- `@serwist/next` bukan `next-pwa`. `sw.js` otomatis dari build.
- Kalau error `Cannot find module` aneh → hapus folder `.next` dulu.
- Gunakan CSS tokens, jangan import tailwind file lain di component.
- Setiap task selesai: pastikan typecheck + build + vitest hijau sebelum lanjut.

---

## SELESAI PROMPT

Lanjutkan dari **T-13 `lib/format.ts` TDD**. Selamat coding! 🎒